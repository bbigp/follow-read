

import 'package:flutter/material.dart';

class ConfirmUnsubscribe extends StatelessWidget {

  const ConfirmUnsubscribe({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
      // DragHandle(),

    ],);
  }

}